#!/bin/bash

#TODO: Check for existance of the mini-CRAN repository. If it is not there, than create.
#Otherwise make sure the submodule is update, and then get the list of packages from it. Check recursively for dependencies and compare them to the list of the packages in the miniCRAN.
#Install those, who are missing
